package vo;
/*
 	CREATE TABLE FAQBoard(
 		idx INT PRIMARY KEY,
 		name VARCHAR(20) FOREIGN KEY(id),
 		passwd VARCHAR(16) NOT NULL,
 		subject VARCHAR(20) NOT NULL,
 		content VARCHAR(2000) NOT NULL,
 		date DATE NOT NULL, 
 		real_file VARCHAR(100),
 		original_file VARCHAR(100),
 		readcount int NOT NULL,
 		category VARCHAR(16) FOREIGN KEY REFERCES TO category(name);
 	);
 */
public class FAQDTO {
	
}
